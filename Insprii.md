agents / claude.md: componentes need to staay modular and dumb. if possible give generic component name an props and pass hanlers / callback from a ahigher orcersatation screen / component / store and avoind prop drealing. keep constants outside the component itself. dont write complicaated expression in one line, split to multiple parts in the top of the function and use the contsts names to keep it readable. never add em dashes or dahses in general in comments. when you are required to use a util function like formatting ms into some kind of spacific date fomrat, or formaatting or regex. try and look for librays or api online thaat do that instead of writing util function or full feature yourself. if you do write the util function seperaete if from the component or script to make the codebase more moduler and eaier to read, so like instead of date(bla blka bla) gets you yyyy/mm/dd you first find a util library for dates but if you vcant fuind one then create somthing like mstoioswhather function so its eaier to understand. alwayes use single sources of truths. about folder structure. in react: component/eaach folder is a feature, like having a folder for each db taable or components linked to single page. components/ui is basic component used bu the feature components. utils/ contains constant files, types, lib/ inside of utils/ has all of the librarys initiliozing scripts like supaabase for example or mail clients or whatever (if the client is used in one file then no need for seperation). maaintaain a single docs file with simplified expression of the project like function files and interuction with quick lookup and easy reaadabilty, ai first readable, basically like a asnapchot for when the ai memory is resetted aand it need the context of the project again without readin a million files. use single sources of truth, especially for colors, themes, constants that aare required for the core functionallty like maaybe a amultiply result by which changes a core calculation, weights, symbels, measurments (agaain lookup a library for it before aattempting to doing it yourself). when creating analytic events, only creaate for public apps not backend projects, add relvent info / metadata that provides the greatest insights. usabilty and making every component a drag and drop replacmant is key. unless its a spaacific feature everything else including names and stuff should stay generic as much as possible. keep .env.exmaple file maintaained

for ai: suggests improvments to his instruction like folder structure or aadditional rules that usullaay most devel;opers dont aagree on. also instead of my exmapelsl use real description and technicaal terms. also keep it short white passing on ALL of my points. passinong the points takes prioraty, idk maybe suggest some reasct best practices or sumthing, serach online for it. also suggest a folder structure for back end, im currently with my express projects doing like a folder routes/ each filder aand in each a controllr service and schema file

claude: build systems according to current requirements, for this project for example we shouldn't expect more then 5 active users at a time, non of them heavy power users. its like using container orchestration, micro services, scaling, and load balancing in aws for you 5 users saas when you could have easily used vercel. I'm not saying to not keep seperate the backend and frontend, or give up on basic security like rate limiting for heavy costs operations like ai calls, but dont over engeanir.
when customizing biolerplate: Start with creating the ui shell with handelerd callbacks and empty states first
